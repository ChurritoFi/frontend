query ValidatorGroupsSuggestion(
  $order_by: [validator_groups_order_by!]
  $limit: Int
) {
  validator_groups(order_by: $order_by, limit: $limit) {
    address
    name
    group_score
    transparency_score
    performance_score
    estimated_apy
  }
}
